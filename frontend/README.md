# DeutschGorilla Frontend

This folder is reserved for the Angular 17+ frontend implementation described in `SPEC.md`.

## Next steps

- Scaffold Angular app (`ng new`) and configure routes/components per spec.
- Implement AI chat, dictionary, flashcards, quiz, and settings views.
- Store AI provider keys in `localStorage` only.
